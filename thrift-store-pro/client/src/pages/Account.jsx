import React,{useState} from "react";import {api} from "../api";
export default function Account({user,setUser}){const [mode,setMode]=useState("login"),[f,setF]=useState({name:"",email:"",password:""}),[err,setErr]=useState("");
if(user)return <main className="page account-page"><p className="eyebrow">MY ACCOUNT</p><h1>Welcome, {user.name}.</h1><p>{user.email}</p>{user.role==="admin"&&<Link className="button dark" to="/admin">ADMIN DASHBOARD</Link>}<button className="outline" onClick={()=>{localStorage.clear();setUser(null)}}>LOG OUT</button></main>;
async function submit(e){e.preventDefault();try{const d=await api("/auth/"+mode,{method:"POST",body:JSON.stringify(f)});localStorage.setItem("thrift_token",d.token);localStorage.setItem("thrift_user",JSON.stringify(d.user));setUser(d.user)}catch(e){setErr(e.message)}}
return <main className="page account-page"><p className="eyebrow">YOUR ACCOUNT</p><h1>{mode==="login"?"Welcome back.":"Join Thrift Store."}</h1><form className="account-form" onSubmit={submit}>{mode==="register"&&<input required placeholder="Full name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/>}<input required type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input required minLength="6" type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/>{err&&<p className="error">{err}</p>}<button className="button dark">{mode==="login"?"LOGIN":"CREATE ACCOUNT"}</button></form><button className="switch" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"Create an account":"Already registered? Login"}</button></main>}